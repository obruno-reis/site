{
  "name": "brunoreis-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site de portfólio do Bruno Reis — gerador estático próprio (sem dependências).",
  "scripts": {
    "build": "node build.js"
  }
}
